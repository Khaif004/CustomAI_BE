# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0

# LangChain & AI
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=1.0.0
langchain-community>=0.2.0
langgraph>=0.1.0

psycopg2-binary>=2.9.9
pgvector>=0.3.0

# Async DB layer for the Tool Registry (additive — existing code stays on psycopg2).
# psycopg v3 is chosen over asyncpg because it consumes the Neon libpq URL
# (sslmode/channel_binding, pgbouncer pooler) verbatim with no URL surgery.
sqlalchemy[asyncio]>=2.0.30
psycopg[binary]>=3.2
greenlet>=3.0

# Data & Utilities
pydantic>=2.7.0
pydantic-settings>=2.2.0
aiohttp>=3.9.0
httpx>=0.27.0

# File parsing
python-multipart>=0.0.9
PyPDF2>=3.0.1
openpyxl>=3.1.0
python-docx>=1.1.0
reportlab>=4.0.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0